des_x, des_y = "", ""


def assign_des(self, btn):
    # btn.text()를 이용해 db에서 조회, 좌표 반환
    pass


def start_driving(self, x, y):
    self.des_x = x  # db에서 조회한 좌표를 변수에 할당
    self.des_y = y
    print(des_x, des_y)

    # 지정좌표로 이동할 수 있는 py파일에 연결


